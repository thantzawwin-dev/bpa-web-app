//import moment from "moment";

// validation
export const isDate = (date: string) => (Date.parse(date) ? true : false);

// format
//export const DateFormat = (datetime: string, format: string) => moment(datetime).format(format);
